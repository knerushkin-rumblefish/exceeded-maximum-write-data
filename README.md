# Error reproduction

To reproduce run:
```bash
yarn deploy
```

on godwoken devnet, using [kicker](https://github.com/RetricSu/godwoken-kicker):

first, run:

```sh
./kicker start
```

then

```bash
yarn deploy:gw-dev
```
